package com.ecomeerce.rest_api.enums;

public enum PaymentStatus {
    pending,
    approved,
    authorized,
    in_process,
    in_mediation,
    rejected,
    cancelled,
    refunded,
    charged_back
}
